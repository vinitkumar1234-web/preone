// config.example.js — TEMPLATE. Safe to commit to GitHub.
// Copy this file to "config.js" (same folder) and fill in your own
// Supabase project's URL and anon key. config.js is gitignored so your
// real credentials never get pushed.
window.APP_CONFIG = {
    SUPABASE_URL: "https://your-project.supabase.co",
    SUPABASE_ANON_KEY: "your-anon-key-here",
    RPC_NAME: "dashboard_json"
};
